export type UserRole = "owner" | "manager" | "csr" | "readonly";
export type NavItem = { label: string; href: string; description?: string };
export type KpiMetric = { label: string; value: string; change: string; tone?: "positive" | "neutral" | "warning" };
export type AlertItem = { id: string; title: string; detail: string; severity: "critical" | "warning" | "info" };
export type ActivityItem = { id: string; title: string; detail: string; timestamp: string };
export type TrendPoint = { label: string; recoveredRevenueCents: number };
export type WorkflowPerformance = { workflow: string; winRate: number; recoveredRevenueCents: number; averageResponseHours: number };
export type OpportunityNote = { id: string; author: string; createdAt: string; body: string };
export type Opportunity = { id: string; title: string; type: string; status: string; priority: string; owner: string; contactName: string; valueCents: number; source: string; lastTouched: string; nextAction: string; notes: OpportunityNote[] };
export type ThreadMessage = { id: string; direction: "inbound" | "outbound"; channel: "sms" | "email"; body: string; timestamp: string };
export type CallRecord = { id: string; caller: string; phone: string; disposition: string; direction: string; startTime: string; durationSeconds: number; summary: string; thread: ThreadMessage[] };
export type EstimateRecord = { id: string; number: string; customer: string; serviceType: string; amountCents: number; status: string; sentAt: string; staleSince?: string; enrolledInFollowUp: boolean };
export type ReactivationSegment = { id: string; name: string; audienceSize: number; estimatedValueCents: number; reason: string; contacts: Array<{ name: string; lastService: string; opportunityValueCents: number }> };
export type CampaignStep = { id: string; order: number; channel: "sms" | "email"; delayHours: number; bodyPreview: string };
export type CampaignRecord = { id: string; name: string; type: string; status: string; audienceLabel: string; launches: string; recoveredRevenueCents: number; steps: CampaignStep[] };
export type ContactRecord = { id: string; name: string; phone: string; email?: string; tags: string[]; lifecycleStage: string; lastServiceDate: string; lastOpportunityValueCents: number };
export type RevenueSummary = { label: string; amountCents: number; delta: string };
export type ConversionSummary = { workflow: string; leads: number; responses: number; bookings: number; winRate: number };
export type TemplateRecord = { id: string; name: string; channel: "sms" | "email"; status: string; subject?: string; body: string; variables: string[] };
export type IntegrationRecord = { id: string; provider: string; status: string; accountLabel: string; lastSync: string; summary: string };
export type BusinessProfile = { name: string; phone: string; website: string; timezone: string; staleEstimateDays: number; attributionWindowDays: number; highValueThreshold: number; duplicateMissedCallWindowHours: number };
export type UserRecord = { id: string; name: string; email: string; role: UserRole; status: string };
export type PermissionMatrixRow = { capability: string; owner: boolean; manager: boolean; csr: boolean; readonly: boolean };
export type NotificationPreference = { id: string; label: string; channel: string; enabled: boolean };
export type DemoStateFrame = { id: string; title: string; summary: string; highlight: string };
export type WalkthroughStep = { id: string; title: string; description: string; outcome: string };
