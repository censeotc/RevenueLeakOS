import { PageLoading } from "@/components/page-loading";

export default function AppLoading() {
  return <PageLoading title="Loading internal app" description="Refreshing the seeded tenant, role guards, and operating pages." />;
}
