import { PageLoading } from "@/components/page-loading";

export default function OnboardingLoading() {
  return <PageLoading title="Loading onboarding" description="Preparing the pilot checklist and seeded operational defaults." />;
}
