import { PageLoading } from "@/components/page-loading";

export default function LoginLoading() {
  return <PageLoading title="Loading demo login" description="Preparing seeded accounts and role-aware sign-in options." />;
}
